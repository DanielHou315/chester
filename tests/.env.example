# Chester Test Environment Configuration
# Copy this file to .env and customize for your environment
# These tests require SSH access and cannot run in CI/CD

# Remote chester repo directories for each mode
# Used by test launchers to sync code before running experiments
CHESTER_REMOTE_DIR_GL=/home/youruser/code/chester
CHESTER_REMOTE_DIR_ARMFRANKA=/home/youruser/code/chester

# Add more modes as needed:
# CHESTER_REMOTE_DIR_MYHOST=/path/to/chester
