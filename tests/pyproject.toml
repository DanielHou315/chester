[project]
name = "chester-tests"
version = "0.1.0"
description = "Chester MNIST test suite"
requires-python = ">=3.8"
dependencies = [
    "chester-ml",
    "torch",
    "torchvision",
    "pytest",
]

[tool.uv.sources]
chester-ml = { path = "..", editable = true }
