[project]
name = "chester-tests"
version = "0.1.0"
description = "Chester MNIST test suite"
requires-python = ">=3.8"
dependencies = [
    "chester",
    "torch",
    "torchvision",
]

[tool.uv.sources]
chester = { path = "..", editable = true }
